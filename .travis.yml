dist: trusty
sudo: false
language: c

# Build only master (and PRs to master)
branches:
  only:
  - master
  submodules: false

# Caching so the next build will be fast too.
cache:
  directories:
  # CABAL
  - $HOME/.cabal/packages
  - $HOME/.cabal/store
  # STACK
  - $HOME/.stack
  - $HOME/.local/bin
  - .stack-work/
  # packages' .stack-work
  - futurice-prelude/.stack-work/
  - ansi-pretty/.stack-work/
  # more packages
  - dynmap-cache/.stack-work
  - futurice-logo/.stack-work
  - futurice-pure-trans/.stack-work/
  - log-logentries/.stack-work/
  - mega-repo-tool/.stack-work
  - monad-http/.stack-work/
  - optparse-sop/.stack-work
  - periocron/.stack-work
  - servant-status/.stack-work/

before_cache:
  # cabal
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/build-reports.log
  # remove files that are regenerated by 'cabal update'
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.*
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/*.json
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.cache
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.tar
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.tar.idx
  # stack
  - rm -rf $(stack --no-terminal --system-ghc --skip-ghc-check path --local-install-root)/bin/*

before_install:
# We use compiler: to tag caches
- export HC=$CC
- unset CC
# job starting moment
- export JOB_START_TIME=$(date +%s)
# paths
- export PATH=/opt/alex/3.1.7/bin:/opt/happy/1.19.5/bin:$PATH
- export PATH=/opt/ghc/$GHCVER/bin:/opt/cabal/head/bin:$PATH
- export PATH=$HOME/.local/bin:$PATH
# Disable hardening
# https://ghc.haskell.org/trac/ghc/ticket/9007
# https://wiki.debian.org/Hardening
- DEB_BUILD_MAINT_OPTIONS=hardening=-all
- dpkg-buildflags --get LDFLAGS
- dpkg-buildflags --get CFLAGS
# Install tools
- scripts/travis.sh prepare $BUILD

install:
- scripts/travis.sh install $BUILD || true

script:
- scripts/travis.sh build $BUILD

matrix:
  fast_finish: true
  include:
# stack unusable because of https://github.com/commercialhaskell/stack/issues/3424
#  - env: BUILD=stack GHCVER=8.0.2 STACK_YAML=stack-ghc-8.0.2.yaml PEDANTIC=YES
#    compiler: ghc-8.0.2
#    addons:
#      apt:
#        sources:
#        - hvr-ghc
#        packages:
#        - libfftw3-dev
#        - alex-3.1.7
#        - happy-1.19.5
#        - ghc-8.0.2
#        - libgmp-dev
#  - env: BUILD=stack GHCVER=8.2.1 STACK_YAML=stack-ghc-8.2.1.yaml # PEDANTIC=YES
#    compiler: ghc-8.2.1
#    addons:
#      apt:
#        sources:
#        - hvr-ghc
#        packages:
#        - libfftw3-dev
#        - alex-3.1.7
#        - happy-1.19.5
#        - ghc-8.2.1
#        - libgmp-dev
  - env: BUILD=cabal GHCVER=8.0.2 PEDANTIC=YES
    compiler: ghc-8.0.2
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - libfftw3-dev
        - alex-3.1.7
        - cabal-install-head
        - happy-1.19.5
        - ghc-8.0.2
        - libgmp-dev
  - env: BUILD=cabal GHCVER=8.2.1 # PEDANTIC=YES
    compiler: ghc-8.2.1
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - libfftw3-dev
        - alex-3.1.7
        - cabal-install-head
        - happy-1.19.5
        - ghc-8.2.1
        - libgmp-dev

